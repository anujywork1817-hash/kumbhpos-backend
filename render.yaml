services:
  - type: web
    name: kumbhpos-backend
    runtime: go
    buildCommand: go build -o server ./cmd/server
    startCommand: ./server
    envVars:
      - key: SERVER_PORT
        value: 8080
      - key: GIN_MODE
        value: release
